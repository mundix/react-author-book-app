import React from 'react'
import PropTypes from 'prop-types'

export const Detail = ({entry}) => {
    return (
        <div>
            <h2>{entry.title}</h2>
        </div>
    )
}

Detail.propTypes = {

}

